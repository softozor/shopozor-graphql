mutation ActivateCustomer($encodedUserId: ID!, $oneTimeToken: String!) {
  customerActivate(encodedId: $encodedUserId, input: {
      token: $oneTimeToken
    }) {
    errors {
      field
      message
    }
  }
}