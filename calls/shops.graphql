query Shops($first: Int!, $after: String = "") {
  shops(first: $first, after: $after) {
    edges {
      node {
        description
        geocoordinates {
          latitude
          longitude
        }
        id
        name
      }
    }
  }
}
