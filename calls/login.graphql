mutation Login($email: String!, $password: String!, $isStaff: Boolean = false) {
  login(email: $email, password: $password, isStaff: $isStaff) {
    errors {
      field
      message
    }
    user {
      id
      permissions {
        code
      }
      isStaff
    }
    token
  }
}
