mutation SignUp($email: String!, $password: String!) {
  consumerCreate(input: {
      email: $email, 
      password: $password,
      isActive: false,
      sendActivationEmail: true
      }) {
    errors {
      field
      message
    }
  }
}
