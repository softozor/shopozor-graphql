mutation ActivateConsumer($encodedUserId: ID!, $oneTimeToken: String!) {
  consumerActivate(encodedId: $encodedUserId, input: {
      token: $oneTimeToken
    }) {
    errors {
      field
      message
    }
  }
}